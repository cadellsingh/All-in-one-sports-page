import React from 'react'

// here is where the user can select which sports they wanna follow

// loop through sports object and if true have a checked mark by it
// add function to change state of selected sports in SidebarSports component

const SelectSports = (props) => {
  const { sports } = props

  return (
    <div>
      ok
    </div>
  )
}

export default SelectSports